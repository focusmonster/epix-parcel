import React from 'react';
import ReactDOM from 'react-dom';

const App: React.FC = () => {
  return <div>This is a test option page</div>;
};

ReactDOM.render(<App />, document.getElementById('root'));
